`session_restore` automatically saves your running apps before shutdown and restores them once you log in, resuming where 
you left.

## Installation
```bash
curl -L https://github.com | tar xz && cd github.com/imrany/session-restore-main && bash scripts/install.sh
```
